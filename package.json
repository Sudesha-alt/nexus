{
  "name": "nexus",
  "private": true,
  "workspaces": ["server", "shared"],
  "scripts": {
    "prepare": "npm run build -w @nexus/shared",
    "dev": "npm run dev -w @nexus/server",
    "build": "npm run build -w @nexus/shared && npm run build -w @nexus/server",
    "lint": "npm run lint -w @nexus/server"
  }
}
